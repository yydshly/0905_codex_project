# Web 演示

这里预留本项目 Web 演示的实现目录。当前尚未创建应用，也没有可访问的演示地址。

接入应用后，在这里记录：

- 技术栈、环境要求与依赖安装命令。
- 本地开发、验证、生产构建与预览命令。
- 构建产物目录、部署平台与静态资源基础路径。
- 已验证的线上地址，以及任何服务端依赖。

使用 GitHub Pages 时，预留路径为 `/0905_codex_project/projects/{{FOLDER}}/`。路径约定见[部署指南](../../../docs/deployment.md)。

发布验证后，将地址写入 `../project.json` 的 `demo` 字段并同步首页。

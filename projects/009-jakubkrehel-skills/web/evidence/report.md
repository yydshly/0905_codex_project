# RUN-001：真实能力图谱的审查、修复与验证

日期：2026-09-06。上游规则版本：267330e1adfc66a718fb65fa6918c1f06d0a689e。执行者是本次对话中的 AI 代理，读取规则并使用文件、终端和浏览器工具；网页展示执行结果，不是在点击时运行模型。

## 输入与范围

输入为已经存在的 009 能力图谱 index.html、styles.css、app.js，不是前一轮人工构建的研究工作台。修改前复制已构建文件到 evidence/before 并计算 SHA-256。快照保留原有问题与优点，不回写改造。

任务范围：搜索、分类、选择技能、查看详情这一完整流程。技术栈为原生 HTML/CSS/JavaScript，没有组件或动画框架。读取了仓库 AGENTS.md、部署约定、项目 README 与 Web 文档。未审查其他子项目，也未将整页所有部分纳入综合审查。

来源：六个 better-* 领域入口、better-interface 与 review-format、break 与 scenarios、variant 与 picker、explain-interface、interface-review。通过直接读取固定版本文件应用流程，没有安装全局插件；这是宿主代理执行文本技能的方式。部分流程适配如下述边界。

## 六领域覆盖

| 领域 | 实际证据 | 结果 |
| --- | --- | --- |
| 无障碍 | 原生按钮、aria-pressed、可见标签、选中状态、键盘焦点与窄屏详情 | 2 项发现；读屏与全面自动审计未验证 |
| 布局 | 原 CSS 的网格、响应式分支；真实页面与固定容器场景 | 已检查范围无独立新发现；未检验全部页面状态 |
| 文案 | 真实搜索无结果提示及恢复路径 | 1 项发现 |
| 排版 | 浏览器计算字号、真实文本、卡片和详情 | 1 项发现；字号属于指南建议，不等于合规判定 |
| 颜色 | 真实计算样式，前景/背景亮度计算 | 1 项发现；仅测所列颜色对 |
| UI | 原 CSS、按钮/选择状态、既有焦点和减少动态效果分支 | 无独立新发现；动画 10% 速率回放未验证 |

## 审查发现与实际修复

路径均相对 projects/009-jakubkrehel-skills/web。

| 严重程度 | 领域 | 原证据位置 | Before | After / 当前位置 | 用户影响 |
| --- | --- | --- | --- | --- | --- |
| HIGH | 颜色 | evidence/before/styles.css:1 | 编号 #767d75 与选中背景 #fff9f1，实测 4.04635:1 | styles.css:10，#5b645b，实测 5.87126:1 | 小字号编号对比度低于本次采用的 4.5:1 基准 |
| HIGH | 无障碍 | evidence/before/app.js:24 附近的卡片渲染与 styles.css:1 | 可见选中提示只有边框和背景颜色，语义状态有 aria-pressed | skill-card.js:5，加入“✓ 已选”，保留 aria-pressed | 选中含义不再仅靠颜色 |
| MEDIUM | 排版 | evidence/before/styles.css:1 | 卡片说明 10px、标识 9px、详情 11px | styles.css:8、9、11，分别 14px、12px、14px | 改善字号可读性，但会增加占用空间 |
| MEDIUM | 文案 | evidence/before/index.html:17 | 空结果只有文字建议，无直接恢复按钮 | index.html:17、app.js:43，清空搜索与分类后恢复 11 项及搜索焦点 | 更容易从无结果状态恢复 |
| MEDIUM | 无障碍 | evidence/before/index.html:15 | 有 aria-label，无持续可见标签 | index.html:15、styles.css:18，可见“搜索技能” | 输入后仍能看到字段用途 |

原始文件的精确行可通过下载快照复核；CSS 原文件采用单行压缩格式。卡片说明原颜色组合 5.30325:1、详情正文 11.13576:1、选中筛选按钮 14.03215:1、首屏主按钮 4.93426:1，本次保留，不随意重绘整个品牌。

## 实际操作与验证

1. 原页观察计算字号，读取卡片编号和背景颜色，计算真实颜色对比度。原始编号 4.046352503795551:1；修改后 5.871258444894432:1。
2. 原页输入“不存在的技能”，0 项且无恢复按钮。修复后点击“清空搜索与分类”，恢复 11 项，焦点 id 为 search。
3. 修复后 320px 浏览器，页面内容宽度 305px（扣除滚动条），没有横向溢出；选择无障碍技能后详情可见。
4. 将真实卡片渲染抽为 skill-card.js，主页面与 stress.js 都导入 renderSkillCard。数据抽为 skill-data.js，不改变既有 11 项资料。
5. break 场景：320px、148px 挤压与实际最长标识、640px、选中状态。一次浏览页面，未见裁剪、重叠或越界，并将观察写回页面。没有虚构组件不存在的加载/错误或用户长文本状态。
6. variant 以信息密度为唯一主轴：紧凑扫描、均衡浏览、阅读优先。使用实际主页面及全部 11 项内容，顶部中立选择器、URL 参数与键盘切换。320px 三方案均为 11 项、文本横向裁剪检查 0、文档无横向溢出。用户尚未选定，定稿及清理未执行。
7. explain-interface 对首屏纸卡读取实际 DOM 与源码：底板 rgb(233,236,229)，网点 radial-gradient 0.7px / 12px 网格，纸卡 rgb(255,254,249)，阴影 0 12px 26px rgba(38,56,43,.05)，桌面矩阵对应 rotate(-1deg)。参数为测量/源码证据，纸面联想是意图推测。

## 变更审查：明确的范围适配

009 在当前工作区尚未提交，不存在可用的旧版本 Git commit。为了不伪造 PR 或历史，本轮使用 git diff --no-index 比较冻结快照与实际当前文件；changes.diff 保留实际差异，新增模块与 /dev/null 比较。此操作是文件快照版的变更审查，不声称完整运行上游 Git/PR 范围解析流程。

实际检查双方：原数据数组移入 skill-data.js；原卡片渲染移入 skill-card.js 并增加可见状态和文本转义；搜索清空作为新增动作；原有筛选、详情、移动端聚焦、流程切换逻辑保留。原按钮内 h3/p 改成 span，保留按钮的完整文本名称。CSS 在原规则后添加局部修复，减少动态效果分支未删除。

直接使用方：主能力图谱；新增共享组件被场景页导入；候选页由主页面生成。没有扩展到独立的旧 lab 工作台或其他子项目。原有已报告问题属于 Pre-existing，本次将其修复；未确认有由本次差异引入的新 HIGH 问题。候选选择器的手机布局在验证中发现过窄，已调整宽度与顶部空间。

## 结论与限制

综合审查：限定已检查的技能图谱流程，报告中的 HIGH 已修复。变更审查在已检查的文件快照范围内未确认新 HIGH；不对未检查的页面、状态和宿主环境作通过声明。

这轮完成了真实输入、规则阅读、审查证据、代码修改和浏览器复核的执行链。没有执行远程 PR 审查、全库所有场景、完整读屏、跨浏览器测试、用户任务耗时对照或通用自动评测。三种候选供用户选择，未代替用户决定优胜方案。原来的 lab 保留为较早的规则示意，不混作本轮证据。

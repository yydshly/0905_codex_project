# 网页验证记录

日期：2026-09-05。环境：Windows，Node.js v22.15.0，实际 Chromium 浏览器。

## 构建与结构

- `node projects/003-wibi-style/web/build.mjs`：通过；28 款唯一调用名、3 条完整流程。
- `node --check projects/003-wibi-style/web/app.js`：通过。
- `node scripts/build-site.mjs`：通过；新项目可被汇总构建，已有启用项目入口一同保留。
- `node scripts/check-site.mjs`：通过；本地入口、资源、研究记录与锚点可解析。
- `node scripts/projects.mjs sync` 与 `check`：通过；项目状态、标签和真实页面封面已同步。
- `git diff --check`：通过。
- 对照上游固定版本 Git tree：28 个 Skill 入口与 28 张缩略图路径共 56 个文件全部存在。

## 实际浏览器交互

| 检查 | 结果 |
| --- | --- |
| 桌面 1280px 首屏 | 四张上游展示图实际载入，标题和入口正常，无横向溢出 |
| 图鉴初始状态 | 28 张风格卡片 |
| 宠物筛选 | 显示 1 款宠物极简线条 |
| 详情弹窗 | 正确显示对应输入、用途、视觉语言和调用示例 |
| 复制按钮 | 点击后出现成功反馈；Clipboard API 调用未报错 |
| 无结果搜索 | 显示空状态与恢复按钮 |
| 清除筛选 | 恢复全部 28 款 |
| 3 条工作流 × 5 步 | 共 15 个步骤的标题与进度对应正确 |
| 流程末步继续 | 回到第一步 |
| 手机 390×844 | 首屏与流程布局正常，无横向溢出 |
| 手机场景筛选 | 显示 4 款场景风格 |
| 手机详情 | 弹窗宽度小于视口，可通过 Escape 关闭 |
| 控制台 | 检查时没有 error / warn |

## 证据与限制

初版截图为实际运行页面，见 `assets/cover.png` 与 `assets/mobile.png`。28 款图鉴远程缩略图仍是上游案例，不是本研究生成结果。新增“实际效果”区另展示本次真实调用产物，以两个 AI 生成输入文件分别传入图像编辑，不用独立图片伪装前后对比。

后续已执行五次内置 image_gen 调用，见 [实验记录](./generation-experiments.md)。这些单次样本与网页测试仍不能推出总体成功率、成本、身份保持精度或跨模型稳定性。静态资源检查不验证所有远程页面未来一直可用。

## 实际效果区追加检查

- 三个场景的按钮状态与展示区域一致，每次只显示对应场景。
- 浏览器实际加载人像输入/输出 1254×1254，餐桌输入/输出与办公室海报 1086×1448，共五个文件。
- 桌面无横向溢出，控制台无 error / warn。
- 手机 390×844 的餐桌输入/输出可并排查看，无横向溢出；通过 Enter 切换办公室场景，海报正常显示。
- 实际效果区新截图见 assets/effects-desktop-v1.png 和 assets/effects-mobile-v1.png，保留初版截图不覆盖。
- 构建读取文件散列清单，与本地每张图片的 SHA-256 对照一致。
- 本项目构建和本地资源检查通过。一次全站构建因并行创建的 005 项目缺少 assets/SOURCES.md 中断，当时仅更新本项目的本地预览产物，未修改其他项目。
- 其他项目文件补齐后重跑全站构建通过，汇总 7 个研究网页；此前的并行创建阻塞已消失。

# 网页验证记录

以下按开发阶段保留历史结果；早期“未生成／未部署”等表述只对应当时阶段，当前交付状态见文末最新记录。

- 日期：2026-09-06。
- 范围：独立能力研究网页，原生 HTML / CSS / JavaScript，无第三方运行依赖。
- 环境：Windows、Node.js 22.15.0、Codex 内置浏览器；本地地址端口 4319。
- 页面规则按研究固定版本整理，属于原创交互说明，不是上游程序的执行结果。

## 构建与交互

已完成 JavaScript 语法检查、全站构建与静态链接检查。汇总包含 9 个网页项目；首次检查通过 270 个本地链接与资源。

| 操作 | 实际结果 |
| --- | --- |
| 切换三项能力 | 标题、说明、输入、输出与边界同步变化 |
| 默认提示词模式 | 模型选择禁用，不调用生图工具 |
| 已登记配置＋001 | 显示名称路径，不传图 |
| 已登记配置＋155 | 显示名称加特征，不传图 |
| 已登记配置＋201 | 显示对应编号参考图 |
| 已登记配置＋210 | 名称优先，空特征不会覆盖名称分支 |
| 未登记模型 | 回退至对应参考图 |
| 输入自定义主题并复制 | 请求内容正确，显示复制成功 |
| 清空主题 | 显示填写提示，复制按钮禁用 |
| 展开源码依据 | 显示固定版本和六个源码链接 |
| 浏览器日志 | 检查时无 error / warn 日志 |

浏览器工具的空字符串填充未实际清除输入，改用全选与退格后确认空主题保护有效。这是测试操作适配，未将无效填充算作页面通过。

## 布局与素材

桌面默认视口检查首屏排版；390 像素窄屏检查单列布局、卡片与页面宽度，未发现页面横向溢出。项目对照表单独横向滚动，并提供滑动说明与键盘焦点入口。首屏小卡片文字已针对窄屏缩小。

封面 assets/cover.png 来自本地实际网页截图。页面中的书本线条、色块与编号卡片由 HTML / CSS 原创绘制，是流程说明，不是上游样图或 AI 生图结果。

## 验证边界

没有进行上游生图调用、216 款逐图检查、完整无障碍审计或跨浏览器回归。网页中的四个编号仅用于解释配置分支，不提供完整风格选择器；自定义主题组件只生成 Agent 请求，不自动翻译或生成最终图片。

已接入仓库构建契约；没有推送或线上部署，project.json 的 demo 保持空值。

## V2：效果展示与真实任务路径（2026-09-06）

将图库与应用场景移至页面前部，原有能力和项目对比折叠。首屏引用 003、059、193 三张上游参考图，图库默认显示 12 款跨类别参考；全部 216 个编号可按类别、搜索和分批加载浏览。

素材按需从固定 commit 的 GitHub Raw 加载，名称索引也在浏览器读取，不在本仓库重新打包全库。中文短名为研究整理，作者／风格参考名称来自上游索引。没有将上游图称为本研究生成成品。

本轮实际检查：

| 操作 | 结果 |
| --- | --- |
| 默认图库与名称索引 | 12 张图片实际解码成功，索引加载成功 |
| G 分类与继续加载 | 先显示 12/16，点击后显示 16/16 |
| 搜索 193 | 显示唯一匹配 |
| 放大与选择 | 193 原图在对话框显示，选择后带入场景区 |
| 活动主视觉任务 | 包含所选 193、主题、4:5、要求及固定来源链接 |
| 编辑主题与比例 | 内容更新到输出任务；复制显示成功 |
| 空主题 | 禁用场景复制按钮 |
| 系列／文章切换 | 更新目标产出、流程和默认需求，保留选中编号 |
| 无结果搜索与重置 | 显示明确提示，重置后恢复默认图库 |
| Escape 关闭预览 | 对话框关闭 |
| 1280 宽屏与 390 窄屏 | 图库、选型与弹窗布局可用，页面无横向溢出 |
| 浏览器日志 | 检查时无 error / warn |
| 构建与资源 | JavaScript 语法、全站构建通过；273 个本地链接与资源通过 |

新增截图为 assets/gallery-v2.png 与 assets/scenario-v2.png，旧 cover.png 保留。场景图只是当前选型参考，三种应用均明确为待出图验证的方案。未执行 216 款逐图视觉评测、网络故障注入或新图生成；不扩大效果结论。

## V3：三个实际场景（2026-09-06）

在 V2 后执行三次内置 image_gen 调用，每次传入对应编号参考图，得到文章配图、三格草案和茶会主视觉。生成范围、尺寸偏差、实际提示词和散列见 assets/generated/REVIEW.md、prompts.json 与 files.json；三张原图无后期处理。

实际样例区置于图库之前，可放大及下载原 PNG。浏览器确认三张本地图片全部解码，尺寸分别为 1672×941、1536×1024、1003×1568；茶会图对话框、标题和下载目标正确，Escape 可关闭。检查时无 error / warn，1280 宽屏与 390 窄屏页面无横向溢出。截图为 assets/examples-v3.png。

JavaScript 语法、生成图片散列、全站构建与 283 个本地链接和资源检查通过。应用不接受网页内实时生图。三格是一次生成，不是跨次一致性实验；比例不符及其他目视差异原样记录，不计为完整交付验收通过。

## 自定义主题与内容输入（2026-09-06）

将主题、画面内容和需要出现的文字分开，保留风格选择、画幅和其他约束。浏览器实填“周末的慢生活”、朋友喝茶聊天的内容和“给生活留一点空白”，验证完整请求保留这些字段与 003 风格；复制成功。清空内容后按钮禁用，切回示例可恢复默认内容。此检查只验证任务组织，不代表新增网页内生图服务。

## GitHub 概述与发布前检查（2026-09-06）

- 新增 scenario-overview.png：三个首轮真实结果的完整排版，1600×1080；对应 HTML 为 web/overview.html。目视确认完整显示三张原图、标题、输入、风格编号、实际用途和页脚边界，卡片内容无溢出。
- 根 README 的项目摘要、索引和单图预览均由 project.json 同步生成；点击概述图进入子项目详情。子项目新增自己的主题／内容如何使用的四步说明。
- 基于最新 origin/main（3a09492）在独立工作区整理，只新增 010 和同步根 README，保留原工作区其他项目的未提交内容与分叉历史。
- 发布前检查通过：10 个项目的元数据与索引校验、9 个网页的完整构建、286 个本地链接与资源、三个应用脚本的语法检查、三张原始生成图 SHA-256 校验。
- 本轮只增加概述与文档，保留 V3 交互验证证据；未新增生成调用或扩大图像质量结论。在线状态在实际部署验证后补录。

## 首次公开部署验证（2026-09-06）

- 内容提交：5bbbf24；已正常推送远端 main，没有强制推送或修改发布工作流。
- GitHub 检查与 [Pages 发布](https://github.com/yydshly/0905_codex_project/actions/runs/34010753900)均成功。
- 浏览器确认 GitHub 根 README 的 010 摘要与图片入口存在；概述图实际解码为 1600×1080，点击链接指向子项目详情。
- [在线实际样例](https://yydshly.github.io/0905_codex_project/projects/010-handraw-style/#examples)可打开，三张图均完整加载，实际尺寸与本地一致；保存截图 assets/online-examples.png。
- 茶会效果放大、Escape 关闭、修改主题、复制完整任务通过。复制内容确实包含新主题；锚点刷新可用，当前桌面页面无横向溢出，检查时无 error / warn。
- 上游索引加载后，请求包含对应风格的生图名称和参考名称。原始 PNG 总体积约 6.3 MB，本次首次打开有明显加载等待；等待完成后再判定图像成功，没有把只显示局部的解码中状态算作通过。
- 线上验证完成后补充 project.json 的 demo 与 README 在线入口。未新增移动端线上回归、弱网故障注入或全库图像质量评测。
